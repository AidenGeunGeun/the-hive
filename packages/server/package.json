{
  "name": "@the-hive/server",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@the-hive/protocol": "workspace:*",
    "@the-hive/workflow": "workspace:*",
    "@the-hive/storage": "workspace:*",
    "@the-hive/config": "workspace:*",
    "@the-hive/providers": "workspace:*",
    "@the-hive/room": "workspace:*",
    "@the-hive/context": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
